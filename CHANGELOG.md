# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Added
- `GEMINI.md` context file for AI assistants.
- Archived `impulse-rs` Rust port attempt to branch `gemini_vOLD`.

### Changed
- Reset `main` branch to stable state (commit `8a782caf`).
- Updated `README.md` with project status and reference to the Rust port attempt.

## [2025-11-21] - Rust Port Attempt
- Created extensive Rust mappings for `records`, `common`, `file`, and `init` units in `gemini_vOLD` branch.
- Encountered persistent compilation errors in `common4.rs`, `file1.rs`, `file11.rs`, and `impdos.rs` preventing a successful build.
- Work archived for future reference.
